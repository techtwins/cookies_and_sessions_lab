<h1>Products</h1>

<ul>
    <% cart.each do |cart| %>
        <li>
            <%= cart %>
        </li>
    <% end %>
</ul>

<%= form_tag add_product_path do %>
    <%= text_field_tag :product %>
    <%= submit_tag "add to cart" %>
<% end %>